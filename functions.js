/*
const coffeeOrder = (size, drinkType) => {
    console.log(`Order received: ${size} ${drinkType}`);
   }
   coffeeOrder("Medium","Latte");
   coffeeOrder("Large", "Black Coffee");
   coffeeOrder("Small", "Espresso")


let orderCount = 0;

const takeOrder = (topping1, topping2, topping3) => {
    console.log(`Pizza with ${topping1}, ${topping2} and ${topping3}`);
    orderCount++;
    console.log(orderCount)
}
takeOrder("ham", "red onion", "mushroom");
takeOrder("pepperoni", "salami", "cheese");
takeOrder("cheese","cheese", "more cheese");

let bal = 300;

const cashWithdrawal = (pin, amount, accnum) => {
    console.log(`Withdrawing ${amount} from account ${accnum}`);

if (pin == 2468 && (amount <= bal)){
    console.log("Transaction Complete")
}
else {
    console.log("Access Denied")
}
}
cashWithdrawal(2468, 50, 83746378)

*/

const sandOrder = (bread, topping1, topping2, topping3, topping4, topping5) => {
    console.log(`Here is your order. One ${topping1}, ${topping2}, ${topping3}, ${topping4} and ${topping5} on ${bread} bread`)

}
sandOrder("white", "salami", "pepperoni", "ham", "lettuce", "tomato",)

